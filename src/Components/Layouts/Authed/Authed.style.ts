import styled from 'styled-components'

export const Layout = styled.div`
  display: flex;
  flex-flow: column;

  min-height: 100vh;
`

export const Content = styled.main`
  display: flex;
  flex: 1;
`
